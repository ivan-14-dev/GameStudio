services:
  - type: web
    name: duoplay-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
