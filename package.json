{
  "name": "duoplay",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Lightweight multiplayer mini-games platform",
  "scripts": {
    "dev": "node server/src/index.js",
    "start": "NODE_ENV=production node server/src/index.js",
    "test": "node --test tests/**/*.test.js server/tests/**/*.test.js"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/static": "^8.0.0",
    "@fastify/websocket": "^11.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/rate-limit": "^10.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
